name: frontend
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  flutter_localizations:
    sdk: flutter  
  get_it: ^7.2.0
  cupertino_icons: ^1.0.2
  flutkit: ^0.0.6
  # flutkit:
  #   path: ../../../../FlutKit/flutkit
  modulary: ^0.0.4
  # modulary:
  #   path: ../../../../Modulary/modulary/
  shared:
    path: ../../shared/frontend/
  catalog:
    path: ../../modules/catalog/frontend

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true